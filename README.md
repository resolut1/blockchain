### 📄 Содержание
- [Что реализовано](#что-реализовано)
- [Как реализовано](#как-реализовано)
- [Установка](#установка)
- [Инструкция по использованию](#инструкция-по-использованию)

# Что реализовано
**🔗 Блокчейн**
- Полная цепочка блоков
- Проверка целостности цепочки (is_chain_valid).
- Сохранение и загрузка цепи на диск. В дальнейшем можно реализовать P2P с запросом к ближайшему узлу для получения самой длинной валидной цепочки в случае сбоя или утери локальных данных.

**🔁 Транзакции**
- Создание и подпись транзакций.
- Проверка валидности через подписи.

**⛏️ Майнинг**:
- Майнится новый блок с проверкой сложности.
- Майнер получает награду в виде новой транзакции с установленным вознаграждением.

**🔐 Кошельки**
- Генерация приватных и публичных ключей.
- Подпись и проверка транзакций.

**🌐 REST API (Flask)**
- Отправка транзакций.
- Запуск майнинга.
- Получение всей цепочки.
- Проверка баланса.

**</> CLI-интерфейс**
- Создание кошелька.
- Отправка транзакции.
- Майнинг.
- Проверка баланса.

---

# Как реализовано
### 🔗 Блокчейн (Blockchain)
**Функции и механика:**

`__init__()` — создаёт цепочку, добавляет генезис-блок:
- Если существует `chain.json`, загружает из файла
- Если файла нет — создаёт первый блок с заглушкой "Genesis Block"

Genesis Block  
:  Это первый блок любого блокчейна, который является основой и прототипом для создания всех остальных блоков.

`create_genesis_block()` — создаёт начальный блок (нулевой):
- Хешируется и добавляется в цепочку

`mine_pending_transactions(miner_address, miner_private_key)`:
- Проверяет все транзакции на валидность
- Создаёт новую транзакцию награды (reward_tx)
- Подписывает её системным ключом
- Создаёт новый блок со всеми неподтверждёнными транзакциями
- Майнит блок (Proof-of-Work)
- Подписывает блок приватным ключом майнера
- Добавляет блок в цепочку и сохраняет на диск
- Очищает пул неподтверждённых транзакций

`is_chain_valid()`: Проверяет:
- правильность хеша каждого блока
- целостность цепочки (previous_hash)
- подпись майнера на каждом блоке

`save_chain()` и `load_chain()`:
- Сохраняют и загружают цепочку в/из файла `chain.json`

### 🔁 Транзакции (Transactions)

**Функции и механика:**

`__init__()` — создаёт новую транзакцию с:
- адрес отправителя (`from_address`)
- адрес получателя (`to_address`)
- сумма (`amount`)
- подпись (`signature` — пока пустая)

`sign_transaction(private_key_hex)`:
- Подписывает транзакцию приватным ключом отправителя
- Превращает её в строку (`to_string`) и добавляет криптографическую подпись

`is_valid()`: Проверяет:
- если это `SYSTEM`, проверяется по системному публичному ключу
- если обычный пользователь, проверяется его подпись

`to_string()`:

Превращает транзакцию в текст, чтобы её можно было:
- Захешировать
- Подписать
- Сохранить

### 🔐 Кошелек (Wallet)

**Функции и механика:**

`generate_keys()` - Генерирует новую пару:
- приватный ключ (`private`)
- публичный ключ (`public`)

`sign_message(private_key_hex, message)`:
- Создаёт цифровую подпись для сообщения

`verify_signature(public_key_hex, message, signature)`:
- Проверяет подпись по публичному ключу и сообщению

### 🌐 REST API

#### Маршруты

`POST /transactions/new` — добавление новой транзакции

`POST /mine` — майнинг новых блоков

`GET /chain` — просмотр всей цепочки

`GET /balance/<address>` — просмотр баланса по адресу

### </> CLI (command line interface)

#### Команды

`create-wallet` — создаёт новый кошелёк

`send` — отправляет транзакцию на сервер

`mine` — запускает майнинг и получает награду

`balance` — показывает текущий баланс по публичному ключу

### ⛏️ Про майнинг

##### Стратегия Proof-of-Work:

- Каждый блок должен начинаться с определённого количества нулей (`0000...`) — это определяется сложностью (`difficulty`)

- Метод `mine_block()` перебирает значения `nonce`, пока хеш не совпадёт с требуемым шаблоном

- Это гарантирует, что на создание блока нужно потратить вычислительные мощности

### 🛡️ Защита системы

- Все блоки подписаны приватными ключами майнеров

- Наградные транзакции подписаны системным ключом

- Любая подмена данных в блоке ломает хеш → нарушает цепочку

- После перезапуска, подписи майнеров проверяются через `is_chain_valid()`

- **СЕЙЧАС** При обнаружении измненения цепочки/блока/любого значения система очищает всю цепочку дабы не пропустить ошибочный блок

- **ПОТЕНЦИАЛЬНО** P2P, запрос самой длинной валидной цепочки как резервной.

---
# Установка
(Предварительно должен быть установлен Python) (Использовалась V3.10.7)
1. Клонировать репозиторий → `git clone https://github.com/resolut1/blockchain.git`

2. Создать виртуальное окружение или установить необходимые библиотеки глобально:
    - Виртуальное окружение **venv** - `python3 -m venv venv`
    Чтобы активировать
    На **Linux/macOS:**
        `source venv/bin/activate`
    На **Windows**:
        `.\venv\Scripts\activate`

    - Установка библиотек в venv (если создано) или глобально -
    `pip install -r requirements.txt`

---

# Инструкция по использованию

1. Запуск файла `run_node.py`
2. Создаем еще одно окно терминала через которое будем использовать **CLI**

Далее все в ваших руках

**Команды**

- `python cli.py create-wallet` - создает новый кошелек, попросит указать его имя

- `python cli.py balance --wallet ИМЯ_КОШЕЛЬКА` - текущий баланс кошелька

- `python cli.py mine --wallet ИМЯ_КОШЕЛЬКА` - запуск майнинга

- `python cli.py send --wallet ИМЯ_КОШЕЛЬКА --to ПУБЛИЧНЫЙ_КЛЮЧ_ПОЛУЧАТЕЛЯ --amount СУММА` - транзакция на отправку монеты (от кого - кому - сумма)
